import { LoadImagePipe } from './load-image.pipe';

describe('LoadImagePipe', () => {
  it('create an instance', () => {
    // const pipe = new LoadImagePipe();
    // expect(pipe).toBeTruthy();
  });
});
